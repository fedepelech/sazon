package com.desarrolloaplicaciones.sazon.ui.theme

import androidx.compose.ui.graphics.Color

val SazonBackground = Color(0xFFFDF7EB)     // Color de fondo principal
val SazonPrimary = Color(0xFFA98C4B)        // Botones y acentos (marrón dorado)
val SazonGreen = Color(0xFF3A9440)          // Texto "Sazón" (verde)
val SazonRed = Color(0xFFD95B3A)            // Ícono y texto de advertencia (rojo anaranjado)
val SazonOrange = Color(0xFFFFA726)         // Detalles de ícono (naranja, opcional)
val SazonInputBorder = Color(0xFFBDBDBD)    // Borde de campos de texto (gris claro)
val SazonTextFieldBackground = Color(0xFFFFFFFF) // Fondo de campos de texto (blanco)
val SazonError = Color(0xFFD32F2F)          // Texto de error ("¿Has olvidado la contraseña?")